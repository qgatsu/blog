# 記事概要
・最初にこの記事でやってることを箇条書きで整理
　・ATE,IWPの解説
　・シミュレーションデータを使ってIPWを実験

# そもそも効果検証では何がしたいのか
・できるだけわかりやすくかつ数式や簡単な具体例を交えて説明する
・本当はY(0),Y(1)を両方観測したいができないため、これをどう近似的に説くかという学問であること。
・ATEとはなにか

# 単純平均差分で簡単にATEを予測してみる
・各軍の平均の差を使った簡単な予測の説明とコードをのせる。
・結果は数値の表と分布の画像で入れる。画像について今はモックでよい。

# 傾向スコア予測
## 傾向スコアとは
・介入群にどうしても生じるバイアスについて具体例を挙げて説明
・傾向スコアがわからないとき（デザインされていない介入が既に与えられてる場合）は機械学習による予測が使える。
・本記事ではXgbiistを使う。

## 傾向スコアの予測
・/home/kohei/WorkSpace/blog/05.effectiveness/02.propencity_score.ipynbをもとにコードと結果を掲載する
・読者はnoteobookを参照できないので、notebookの存在を記事では明示しない。

・モデルの作成、予測についてコードを乗せる。とくにk-fold的にOOFで予測して補完していることは説明しておく。
・メトリクスを表で、分布を画像で載せる、画像は今はモックでいい。

# IWP（逆重みづけ法）
## IWP（逆重みづけ法）とは
・どういう思想で生まれた手法か（当時の背景や論文を簡単に交えられるとなおよい。）
・数理的に何をしているか。逆重みづけ法という名称との接続が読者の頭の中でできるのがベスト。

## IWPの実装
・さっき予測した傾向スコアを使ってIWPを実装する
・重みづけの処理についてコードを乗せる。
・平均差分とのMAE比較を数字の表と分布の画像で行う、画像はモック。

## データ数を変えた場合のIWPの挙動
・実験設定の説明
・結果を表と画像で載せる。
・簡単な考察

# まとめ




